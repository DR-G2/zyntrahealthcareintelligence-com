

## Plan: Increase Watermark Visibility

Bump the watermark text opacity from `0.035` (light) / `0.045` (dark) to `0.055` (light) / `0.065` (dark) in `src/components/SecurityOverlay.tsx` line ~113.

Single line change in the `className` of the watermark `<span>` elements.

